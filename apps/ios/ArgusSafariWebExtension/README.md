# Argus Safari Web Extension iOS

The iOS Safari Web Extension mirrors the macOS browser-native path where Apple
permits it. It should emit page title, URL/domain, and explicit selection text
only after the user enables the extension.

Browser extension context should take precedence over OCR or screenshots.
