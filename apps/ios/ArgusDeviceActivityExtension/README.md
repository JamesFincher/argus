# Argus DeviceActivity Extension

DeviceActivity support is optional and aggregate-only.

Allowed:

- App/category usage aggregates.
- Scheduled reporting windows.
- User-visible controls and permission status.

Not allowed:

- Raw screen contents.
- Hidden app-by-app surveillance outside Apple-approved APIs.
- Attempts to bypass FamilyControls approval requirements.
