# Argus File Provider Extension

The File Provider target is optional and should back an Argus-controlled local
working set rather than broad disk inspection.

Scope:

- Represent an optional "Argus Memory" domain.
- Sync user-approved artifacts and metadata.
- Keep raw documents local and policy-gated.
- Prefer file metadata and user-selected context over continuous content reads.
